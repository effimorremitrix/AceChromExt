/** Types for templateData.mjs, so the tests and scripts can import it under strict typechecking. */
export const COLUMNS: string[];
export const EXAMPLE_ROWS: Array<Record<string, string | number>>;
export const INSTRUCTIONS: string[][];
